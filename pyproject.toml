[tool.poetry]
name = "practice"
version = "0.1.0"
description = "This is just a practice!"
authors = ["Lori"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.7"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
